# General Relativity and the KWE Medium — Concept Conclusions

*Measured conclusions from the probe suite, live instruments, and the signaling/orbit experiments
(medium.js, 2026-07-02). Every law below is backed by a control-gated measurement; none is asserted
from theory alone.*

---

## I. The central thesis

**KWE realizes the structure of General Relativity by selection, not by construction.**
It does not discretize Einstein's equations. It is a self-sustaining nonlinear wave medium in which
the GR relationships are the *only forms that survive* the dynamics:

- **Geometry is the only mover of persistent matter** — not postulated (as the equivalence
  principle is in GR) but *enforced*: every force-like mover was measured inert (8 phase-well
  strengths, 4 momentum tilts), while coordinate transformations move matter freely.
  The later-found cause: the **mobility wall** — a stable dissipative soliton is pinned by its own
  sustain (the gain-threshold acts as a frozen flame-front at its boundary). Matter that persists
  *cannot* move itself; only a relabeling of space moves it.
- **Geometry and matter form a closed loop** — matter tells geometry (the vortex mode's genome
  slides toward the field's energy centroid; the coevolve leash paces the operator by the matter's
  lock), geometry tells matter (basins hold, injection entrains, warp translates). Both directions
  measured with controls. This is the Einstein loop — "matter tells spacetime how to curve,
  spacetime tells matter how to move" — running as a discrete, replicated dynamical system.

The dual-layer architecture (geometric operator layer ⇄ energy field layer) is therefore not an
implementation detail: it *is* the medium's general relativity.

## II. The measured laws

### 1. Two matter species (the γ dial)

| | bound / heavy (γ≈20) | mobile / radiation-like (γ≈1 + CGL) |
|---|---|---|
| persistence | immortal (mass ×1.00 over 1000+ steps) | marginal, transient — dies or must be re-fed |
| mobility | **zero** (a direct κ=0.8 kick moves it 0.001 px/step) | ballistic; band-structured v(k) on the ring substrate |
| momentum | no momentum channel exists | real (82 % ballistic handoff after trap release) |
| moved by | the operator only (position channel) | its own momentum, while it lives |

**The mobility wall:** there is no species between these — the dissolve↔pinned transition is
first-order with no mobile-stable window (swept in ε, μ, δ, self-focus alternative, bare vs rings).
Translation of a dissipative soliton requires living at the gain margin; the margin is what kills it.
*Mobility and persistence are one dial pointing opposite ways.*

### 2. The position channel has no speed limit

Coordinates carry no inertia. The operator moved matter's pattern at 1148 px/bar with full lock —
the analog of superluminal metric evolution (inflation): *nothing moves through space faster than
the medium allows, but space itself may be commanded at any rate.* What limits the outcome is not
speed but memory (law 4).

### 3. Vacuum class and the soliton sector

The empty medium's dispersion is **Schrödinger-class** (ω ∝ k², exponent 2.00 measured; no
Lorentzian cone), so the textbook acoustic-metric (Unruh) construction does not attach to the
vacuum. The relativistic structure lives elsewhere:
- in the **mobile-matter sector**: velocity saturates with momentum (v → v* as κ grows — the
  relativistic v(p) shape) on the bare substrate; on the native ring kernel the velocity is
  band-structured (dispersion pockets);
- in the **clock layer** (law 6).

### 4. Conservation and its consequences (the halo law)

Under the global energy cap, perturbation energy **cannot die** — a message's energy becomes a
long-lived halo; traffic *heats* the channel; every observable that reads levels is eventually
fooled. Consequences, all measured:
- signals are detected by **edges against continuously-tracked baselines**, never by levels;
- time-division channels need guard intervals ≥ the full event (injection + propagation + echo +
  lingering ≈ 300 steps) — *the medium's memory sets the clock, not vice versa*;
- any closed-loop reference that includes the operator's own output self-fools; only
  globally-normalized quality observables (the lock) are honest;
- **differences are conserved too (the mirror experiment, Lyapunov ≈ 0).** A virtualized copy of
  the world run under the *identical* drive holds its overlap with the world flat: V⊗ ≈ 0.93 for
  46 bars, d ln(1−V⊗)/dbar = −0.0004 — neither healing nor chaos (|λ| < 10⁻³/bar). The drive
  enforces agreement only in the subspace it commands (the ~93 % common mode, converged within τ);
  the residual — the lift's reconstruction error, redistributed as halo — lives in the free
  subspace, where the cap forbids it to die and the dynamics do not amplify it. The medium is
  dissipative toward its command and conservative in everything else: *τ forgets perturbations
  relative to the drive; nothing forgets a difference.*

### 5. Memory time τ — the medium's central material constant

**Definition.** τ is the time over which the medium *forgets a perturbation and re-crystallizes
toward the operator's current command* — equivalently, the persistence time of any local
disturbance, and the coupling constant between the geometry layer and the energy layer.

**Triangulated by four independent instruments:**

| instrument | measured |
|---|---|
| signal lingering after a flash (SCANARM) | ~140 steps |
| reactor echo-cascade window | ~150–210 steps |
| orbital low-pass smearing (radius contraction 0.70 = sinc of the smear angle) | ~60–130 steps |
| channel guard requirement (full event) | ~300 steps incl. propagation |

τ ≈ **100–150 steps** (≈ seconds at present clock rates) for the core constant, with the full-event
envelope ≈ 2τ.

**What τ means in the GR analogy:** it replaces inertial mass. Bound matter does not resist
*acceleration* (it has no momentum to conserve); it resists *forgetting*. A command faster than 1/τ
is not refused and not chased — it is executed **as its time-average** (measured: a circle swept at
ω ≫ 1/τ is orbited at radius R·sinc(ω·τ/2) ≈ 0.70 R, constant across R). Hence the medium's
equation of motion for bound matter is not F = ma but:

> **state(t) → low-pass_τ( geometry-command(t) )** — *geometry commands, matter remembers.*

Inertia is the memory of velocity; this medium's matter has no velocity memory, only position
memory with time constant τ. That is the precise, quantitative content of the phrase.

### 6. The clock layer: native Kepler (fractal time)

Kepler's laws, stripped of Newton, are two statements about clocks: an **occupation law** (K2: how
the cycle spends its time vs distance) and a **scale-period law** (K3: cycle time vs cycle size).
In KWE these live in the layer that owns time — the fractal IFS clock — and are measured:

- **Scale-period law: log-periodic.** Beat delays contract per depth by ratios drawn from the
  clock's map set — a *discrete scale hierarchy*, not a power law. The constants **are the
  geometry's contraction ratios** (pentagonal, silver, dyadic, golden, octagonal, hexagonal in the
  default spectrum). Verified by editing the world: a golden-only clock produced φ-spaced kernel
  rings (11→7 and 8→5, ratios 0.64/0.625) and a delay-ladder fit of r* = 0.640 ≈ φ⁻¹ (residual
  explained by two-root comb interference). **The world's "gravitational constant" is editable
  geometry.**
- **Occupation law with a causal floor.** IFS_MIN_DELAY is a Planck-like cutoff (in ring units
  0.25/0.06 ≈ 4.2 — exactly the smallest kernel ring observed). It prunes fast branches (survivor
  bias in the measured ratios) and UV-cuts the small-scale end of the occupation.
- **The scale-period exponent of the beat→ring map is 1** (delay ∝ radius): the *causal*,
  light-cone scaling — a wave medium's answer, where gravity's would be 3/2.

The dragged-orbit experiments measured layer *coupling* (τ), not orbital law; the orbital laws were
in the clock all along. **"The operator is the curvature" extends to the time domain: geometry sets
the clock spectrum, and the optics (kernel) follow.**

### 7. Radiation, signaling, and horizons

- In vacuum a launched packet is a **one-bar broadband flash** (spreads 30–40 px in one bar, no
  condensation at any amplitude — dispersion outruns self-focus). Near an object it may **condense
  into immortal satellite matter** (the bound phase adopts it) — the matter/radiation distinction
  again, now for emissions.
- An object's near-field **absorbs its own emissions within ~20 px**; signaling launches from the
  vacuum boundary.
- The horizon/causality structure lives in the **momentum/β channel** (the Adler locking threshold
  β ≈ 0.1 ≡ the adiabatic trap-transport limit), not in continuous-drag locks — a slowly dragged
  pattern is re-created at any speed and never "crosses" anything.
- A complete honest channel was built on these laws (emitter → flash → CFAR edge receiver →
  replicated reaction), with every protocol constant a measured property of the medium.

### 8. Determinism as the covariance principle

Peer identity is defined on two tiers: **byte-exact** state (the strict gate, holds across peers
through kernel changes, signals, reactions — via stamped event queues and derived-state loops
computed from the shared field at shared steps) and **invariant** state (winding, centroid cell,
energy — the macro tier, the observables that survive hardware noise). The reflector carries only
*intents*; all reactions are derived locally and identically — the KWE idiom *ARM replicated,
TRANSITION derived*. This is the medium's version of observer covariance: no peer is privileged,
and "the same physics" is checkable at both the coordinate and the invariant level.

## III. What KWE-GR is *not* (the honest boundary)

- **No local energy conservation** — the cap is a global controller (∇·T = 0 fails locally); the
  halo law is the visible consequence.
- **No diffeomorphism invariance** — there is a preferred grid and frame.
- **The geometry-response law is programmed** (lr-paced relaxation, gated decisions), not derived
  from an action principle. GR-shaped, not GR-derived. (The natural next rung: derive the genome
  update as extremization of a functional of the field.)
- **No gravitational waves** — geometry parameters update globally; only the *observable* attractor
  re-forms at the finite contraction rate (an emergent, partial causality of geometry).
- **No Newtonian limit** — ω(R) orbital dynamics cannot exist for bound matter (no inertia);
  the medium's Kepler is the log-periodic clock law instead.

## IV. Closing statement

KWE is an analog general-relativistic medium of an unconventional kind. It reaches GR's two central
facts — *geometry is the only mover; geometry and matter close a loop* — not by imitating the
field equations but because its own dynamics eliminate every alternative. Where it departs from
textbook GR, the departures are not defects but the signature of a consistent alternative physics:

- **two matter species** instead of one (persistent-pinned and mobile-transient),
- **memory in place of inertia** (the constant τ, replacing mass as matter's resistance),
- **fractal, log-periodic time** in place of continuous time (with a causal floor as its Planck
  scale),
- **editable constants** in place of universal ones (the clock's Kepler spectrum is the genome).

The medium does not simulate a universe. It is one, with its own laws — and, decisively, those laws
were *measured from inside*, by instruments built of the same medium, each instrument corrected by
the world it was measuring until it told the truth.

---
